import SectionUniqueGallery from '../../Components/SectionUniqueGallery/SectionUniqueGallery';
import styles from './GalleryUnique.module.scss';

const GalleryUnique = () => {
  return (
    <main className={styles.main}>
      <SectionUniqueGallery />
    </main>
  );
};

export default GalleryUnique;
